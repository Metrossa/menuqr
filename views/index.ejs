<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TRSK — Digital Menu</title>
    <meta name="description" content="Minimalist, premium digital menu for TRSK boutique coffee shop.">
    <link rel="stylesheet" href="/style.css">
    <style>
        /* Enhanced Typography & Cultural Polish */
        .ar {
            font-family: 'Cairo', 'Noto Naskh Arabic', 'Tajawal', Arial, sans-serif;
            font-size: 0.95rem;
            color: #666;
            direction: rtl;
            text-align: right;
            margin-top: 0.15em;
            font-weight: 400;
            letter-spacing: 0;
            line-height: 1.4;
        }
        
        /* Modular Block Structure */
        .menu-section {
            margin-bottom: 4rem;
        }
        
        .section-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 2rem;
            padding-bottom: 0.75rem;
            border-bottom: 2px solid #111;
        }
        
        .section-icon {
            font-size: 1.25rem;
            opacity: 0.8;
        }
        
        .section-title {
            font-size: 1.75rem;
            font-weight: 800;
            color: #111;
            letter-spacing: -0.02em;
            margin: 0;
        }
        
        .section-title-left {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
        
        .section-title-arabic {
            font-family: 'Cairo', 'Noto Naskh Arabic', 'Tajawal', Arial, sans-serif;
            font-size: 1.4rem;
            font-weight: 600;
            color: #666;
            letter-spacing: 0;
            text-align: right;
            direction: rtl;
        }
        
        /* Menu Blocks with Dividers */
        .menu-blocks {
            display: flex;
            flex-direction: column;
            gap: 0;
        }
        
        .menu-block {
            display: grid;
            grid-template-columns: 1fr auto 1fr;
            align-items: center;
            padding: 1.5rem 0;
            border-bottom: 1px solid #eee;
            position: relative;
            gap: 2rem;
        }
        
        .menu-block:last-child {
            border-bottom: none;
        }
        
        /* Item Information */
        .item-info {
            display: flex;
            flex-direction: column;
        }
        
        .item-name {
            font-size: 1.25rem;
            font-weight: 700;
            color: #111;
            margin-bottom: 0.2em;
            letter-spacing: -0.01em;
            line-height: 1.3;
        }
        
        .item-name-arabic {
            font-family: 'Cairo', 'Noto Naskh Arabic', 'Tajawal', Arial, sans-serif;
            font-size: 1.1rem;
            color: #666;
            font-weight: 500;
            letter-spacing: 0;
            line-height: 1.4;
            text-align: right;
        }
        
        .item-description {
            font-size: 0.9rem;
            color: #888;
            font-style: italic;
            margin-top: 0.3em;
            line-height: 1.3;
        }
        
        /* Price Container */
        .price-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-width: 80px;
            gap: 0.2rem;
        }
        
        .item-price {
            font-size: 1.2rem;
            font-weight: 600;
            color: #111;
            font-family: 'Inter', sans-serif;
            white-space: nowrap;
            display: flex;
            align-items: center;
            gap: 0.3rem;
        }
        
        .sar-icon {
            width: 16px;
            height: 16px;
            object-fit: contain;
        }
        
        .item-calories {
            font-size: 0.8rem;
            color: #888;
            text-align: center;
        }
        
        .item-allergens {
            font-size: 0.75rem;
            color: #d32f2f;
            font-weight: 500;
            text-align: center;
        }
        
        /* Brand Header */
        .brand-header {
            text-align: center;
            margin-bottom: 3rem;
            padding-bottom: 2rem;
            border-bottom: 1px solid #eee;
        }
        
        .logo-container {
            margin-bottom: 1rem;
        }
        
        .brand-logo {
            max-width: 400px;
            height: auto;
            margin: 0 auto;
            display: block;
        }
        
        .brand-title {
            font-size: 2.5rem;
            font-weight: 900;
            color: #111;
            margin-bottom: 0.2em;
            letter-spacing: -0.03em;
        }
        
        .brand-subtitle {
            font-size: 1.1rem;
            color: #666;
            font-weight: 400;
            margin-bottom: 0.5em;
        }
        
        .brand-tagline {
            font-size: 0.9rem;
            color: #888;
            font-style: italic;
        }
        
        /* Footer */
        .menu-footer {
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid #eee;
            text-align: center;
        }
        
        .footer-text {
            font-size: 0.9rem;
            color: #888;
            margin-bottom: 0.5em;
        }
        
        .footer-brand {
            font-size: 1rem;
            color: #111;
            font-weight: 600;
        }
        
        /* Responsive Design */
        @media (max-width: 600px) {
            .menu-block {
                grid-template-columns: 1fr;
                gap: 0.5rem;
                text-align: center;
            }
            
            .item-info {
                text-align: center !important;
                display: flex;
                flex-direction: column;
            }
            
            .item-info[style*="text-align: right"] {
                order: -1;
                margin-bottom: 0.3rem;
            }
            
            .item-info:not([style*="text-align: right"]) {
                order: 0;
            }
            
            .item-name-arabic {
                text-align: center !important;
            }
            
            .price-container {
                order: 1;
                margin: 0.5rem 0;
            }
            
            .brand-title {
                font-size: 2rem;
            }
        }
        
        /* Accessibility */
        button:focus, input:focus, select:focus {
            outline: 2px solid #111;
            outline-offset: 2px;
        }
    </style>
    <!-- Enhanced Arabic Typography -->
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&family=Inter:wght@400;600;700;800;900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <!-- Brand Header -->
        <header class="brand-header" role="banner">
            <div class="logo-container">
                <img src="/images/logo.png" alt="TRSK Logo" class="brand-logo">
            </div>
            <div class="brand-tagline">Boutique Coffee & Culture</div>
        </header>

        <!-- Hot Drinks Section -->
        <% const hotDrinks = menu.filter(item => item.category === 'hot'); %>
        <% if (hotDrinks.length > 0) { %>
        <section class="menu-section">
            <div class="section-header">
                <div class="section-title-left">
                    <span class="section-icon">☕</span>
                    <h2 class="section-title">Hot Drinks</h2>
                </div>
                <h2 class="section-title-arabic">مشروبات حارة</h2>
            </div>
            <div class="menu-blocks">
                <% hotDrinks.forEach(item => { %>
                <div class="menu-block">
                    <div class="item-info">
                        <div class="item-name"><%= item.name %></div>
                        <% if (item.description) { %>
                        <div class="item-description"><%= item.description %></div>
                        <% } %>
                    </div>
                    <div class="price-container">
                        <span class="item-price">
                            <img src="/images/sar.png" alt="SAR" class="sar-icon">
                            <%= item.price.toFixed(2) %>
                        </span>
                        <div class="item-calories"><%= item.calories %> Cal</div>
                    </div>
                    <div class="item-info" style="text-align: right;">
                        <div class="item-name-arabic"><%= item.arabic_name %></div>
                    </div>
                </div>
                <% }); %>
            </div>
        </section>
        <% } %>

        <!-- Cold Drinks Section -->
        <% const coldDrinks = menu.filter(item => item.category === 'cold'); %>
        <% if (coldDrinks.length > 0) { %>
        <section class="menu-section">
            <div class="section-header">
                <div class="section-title-left">
                    <span class="section-icon">🧊</span>
                    <h2 class="section-title">Cold Drinks</h2>
                </div>
                <h2 class="section-title-arabic">مشروبات باردة</h2>
            </div>
            <div class="menu-blocks">
                <% coldDrinks.forEach(item => { %>
                <div class="menu-block">
                    <div class="item-info">
                        <div class="item-name"><%= item.name %></div>
                        <% if (item.description) { %>
                        <div class="item-description"><%= item.description %></div>
                        <% } %>
                    </div>
                    <div class="price-container">
                        <span class="item-price">
                            <img src="/images/sar.png" alt="SAR" class="sar-icon">
                            <%= item.price.toFixed(2) %>
                        </span>
                        <div class="item-calories"><%= item.calories %> Cal</div>
                    </div>
                    <div class="item-info" style="text-align: right;">
                        <div class="item-name-arabic"><%= item.arabic_name %></div>
                    </div>
                </div>
                <% }); %>
            </div>
        </section>
        <% } %>

        <!-- Black Coffee Section -->
        <% const blackCoffee = menu.filter(item => item.category === 'black'); %>
        <% if (blackCoffee.length > 0) { %>
        <section class="menu-section">
            <div class="section-header">
                <div class="section-title-left">
                    <span class="section-icon">⚫</span>
                    <h2 class="section-title">Black Coffee</h2>
                </div>
                <h2 class="section-title-arabic">قهوة سوداء</h2>
            </div>
            <div class="menu-blocks">
                <% blackCoffee.forEach(item => { %>
                <div class="menu-block">
                    <div class="item-info">
                        <div class="item-name"><%= item.name %></div>
                        <% if (item.description) { %>
                        <div class="item-description"><%= item.description %></div>
                        <% } %>
                    </div>
                    <div class="price-container">
                        <span class="item-price">
                            <img src="/images/sar.png" alt="SAR" class="sar-icon">
                            <%= item.price.toFixed(2) %>
                        </span>
                        <div class="item-calories"><%= item.calories %> Cal</div>
                    </div>
                    <div class="item-info" style="text-align: right;">
                        <div class="item-name-arabic"><%= item.arabic_name %></div>
                    </div>
                </div>
                <% }); %>
            </div>
        </section>
        <% } %>

        <!-- Desserts Section -->
        <% const desserts = menu.filter(item => item.category === 'dessert'); %>
        <% if (desserts.length > 0) { %>
        <section class="menu-section">
            <div class="section-header">
                <div class="section-title-left">
                    <span class="section-icon">🍰</span>
                    <h2 class="section-title">Desserts</h2>
                </div>
                <h2 class="section-title-arabic">حلويات</h2>
            </div>
            <div class="menu-blocks">
                <% desserts.forEach(item => { %>
                <div class="menu-block">
                    <div class="item-info">
                        <div class="item-name"><%= item.name %></div>
                        <% if (item.description) { %>
                        <div class="item-description"><%= item.description %></div>
                        <% } %>
                    </div>
                    <div class="price-container">
                        <span class="item-price">
                            <img src="/images/sar.png" alt="SAR" class="sar-icon">
                            <%= item.price.toFixed(2) %>
                        </span>
                        <div class="item-calories"><%= item.calories %> Cal</div>
                        <% if (item.allergens) { %>
                        <div class="item-allergens"><%= item.allergens %></div>
                        <% } %>
                    </div>
                    <div class="item-info" style="text-align: right;">
                        <div class="item-name-arabic"><%= item.arabic_name %></div>
                    </div>
                </div>
                <% }); %>
            </div>
        </section>
        <% } %>

        <!-- Beverages Section -->
        <% const beverages = menu.filter(item => item.category === 'beverages'); %>
        <% if (beverages.length > 0) { %>
        <section class="menu-section">
            <div class="section-header">
                <div class="section-title-left">
                    <span class="section-icon">💧</span>
                    <h2 class="section-title">Beverages</h2>
                </div>
                <h2 class="section-title-arabic">مشروبات</h2>
            </div>
            <div class="menu-blocks">
                <% beverages.forEach(item => { %>
                <div class="menu-block">
                    <div class="item-info">
                        <div class="item-name"><%= item.name %></div>
                        <% if (item.description) { %>
                        <div class="item-description"><%= item.description %></div>
                        <% } %>
                    </div>
                    <div class="price-container">
                        <span class="item-price">
                            <img src="/images/sar.png" alt="SAR" class="sar-icon">
                            <%= item.price.toFixed(2) %>
                        </span>
                        <div class="item-calories"><%= item.calories %> Cal</div>
                    </div>
                    <div class="item-info" style="text-align: right;">
                        <div class="item-name-arabic"><%= item.arabic_name %></div>
                    </div>
                </div>
                <% }); %>
            </div>
        </section>
        <% } %>

        <!-- Footer -->
        <footer class="menu-footer" role="contentinfo">
            <div class="footer-text">All prices in SAR &middot; جميع الأسعار بالريال السعودي</div>
            <div class="footer-brand">TRSK &mdash; ترسك</div>
        </footer>
    </div>
</body>
</html>
